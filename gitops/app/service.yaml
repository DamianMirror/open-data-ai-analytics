apiVersion: v1
kind: Service
metadata:
  name: ukraine-web
  namespace: ukraine-app
spec:
  type: NodePort
  selector:
    app: ukraine-web
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
